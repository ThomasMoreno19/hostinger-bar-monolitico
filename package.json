{
  "name": "wincoffee-fullstack",
  "private": true,
  "scripts": {
    "build": "cd front && npm install && npm run build && cd ../back && npm install && npm run build",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}