{
  "name": "wincoffee-fullstack",
  "private": true,
  "scripts": {
    "build:front": "npm --prefix front install && npm --prefix front run build",
    "build:back": "npm --prefix back install && npm --prefix back run build",
    "build": "npm run build:front && npm run build:back",
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}